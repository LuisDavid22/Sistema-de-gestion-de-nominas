@model IEnumerable<Sistema_de_gestion_de_nominas.Models.Payroll>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Listado de empleados</h1>
<br />
<button class="btn btn-primary">@Html.ActionLink("Crear Nomina", "Edit", "Payroll", new { id = 0 }, new { style = "color:white" })</button>
@if (Model.Count() == 0)
{
    <h3>No hay ninguna nomina, @Html.ActionLink("crea una!", "Edit", "Payroll") </h3>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Empleado Id</th>
                <th scope="col">Sueldo Bruto</th>
                <th scope="col">AFP</th>
                <th scope="col">ARS</th>
                <th scope="col">Sueldo imponible</th>
                <th scope="col">ISR</th>
                <th scope="col">Total retencion</th>
                <th scope="col">Sueldo neto</th>
                <th scope="col">Opciones</th>
            </tr>
        </thead>
        @*<tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <th>@employee.Id</th>
                    <td>@employee.Name</td>
                    <td>@employee.LastName</td>
                    <td>@employee.Genre</td>
                    <td>@string.Format("{0:N0}", employee.GrossSalary) </td>
                    <td>@{var text = employee.Active ? "Si" : "No";} @text </td>
                    <td><button class="btn btn-warning">@Html.ActionLink("Editar", "Edit", "Employees", new { id = employee.Id }, new { style = "color:white" })</button></td>
                    <td><button class="btn btn-danger">@Html.ActionLink("Eliminar", "Delete", "Employees", new { id = employee.Id }, new { style = "color:white" })</button></td>
                </tr>
            }
        </tbody>*@
    </table>
}






